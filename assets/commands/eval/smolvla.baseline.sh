lerobot-record \
  --robot.type=so_tactile_follower \
  --robot.port=/dev/ttyACM0 \
  --robot.id=follower_arm \
  --robot.cameras='{top: {"type": "opencv", "index_or_path": 0, "width":640, "height":480, "target_width":224, "target_height":224, "fps":30}}' \
  --teleop.type=so100_leader \
  --teleop.port=/dev/ttyACM1 \
  --teleop.id=leader_arm \
  --dataset.repo_id=${HF_USER}/eval_smolvla_baseline_pen_bag \
  --dataset.fps=10 \
  --dataset.num_episodes=50 \
  --dataset.episode_time_s=30 \
  --dataset.reset_time_s=10 \
  --dataset.single_task="Grab the pen from the bag and place it in the basket next to it" \
  --policy.path=YOUR_CHECKPOINT_PATH
